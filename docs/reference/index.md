# Old HTML and Perl/CGI based site

This directory contains [the files from the existing legacy system](./old_cgi_site)

Run the following from time to time to grab the latest data files.

From the `docs/reference` directory

```bash
scp 'danheller.com:public_html/afg/commitment/save/*' ./old_cgi_site/commitment/save/
```
